# The lockfile is resolved with legacy peer-dep behaviour (a transitive peer
# conflict in the eslint-config-next / recharts / react tree). Pin it here so
# `npm ci` on CI, Vercel and fresh clones installs the same tree that
# `package-lock.json` describes — otherwise strict peer resolution rejects it.
legacy-peer-deps=true
